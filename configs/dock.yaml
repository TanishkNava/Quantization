# Dock detector — static quantization pipeline
model_name: dock

# Required inputs (paths relative to repo root)
#exp_file: exps/dock/yolox_voc_s3.py
exp_file: /home/navavision/Utkarsh-Workspace/Quant/exps/dock/yolox_voc_s3.py
weights: models/dock/weights/best_ckpt.pth
classes: models/dock/classes.txt
calibration_dir: calibration/dock

# Model / export
input_size: 640
opset: 18

# Static quant
max_calib_images: 100

# Outputs (defaults: artifacts/dock/fp32.onnx, artifacts/dock/int8.onnx)
output_dir: artifacts/dock

# Inference defaults
conf_thres: 0.25
nms_thres: 0.45
